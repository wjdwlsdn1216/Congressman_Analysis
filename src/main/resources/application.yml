
spring:
  datasource:
    url: jdbc:oracle:thin:@(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1521)(host=adb.ap-chuncheon-1.oraclecloud.com))(connect_data=(service_name=gb75c9302b07ccd_goharddb_medium.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
    username: ROADKING
    password: Gohard1267!!
    driver-class-name: oracle.jdbc.OracleDriver

  devtools:
    livereload:
      enabled: true
  thymeleaf:
    cache: false


  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100 # in (?,?, ... )  in 쿼리로 한번에 땡겨오는 데이터 개수

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

#server 설정
server:
#  port: 8080
  port: 50000 #linux 서버컴 포트

logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
